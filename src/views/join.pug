extends base

block contents  
    form(method="POST")
        input(name="username" type="text" placeholder="Insert your ID" minlength=8 required)
        input(name="email" type="email" placeholder="E-mail" required)
        input(name="password" type="password" placeholder="password" required)
        input(name="checkpass" type="password" placeholder="Checking password" required)
        input(name="name" type="text" placeholder="Your Name" required)
        input(type="submit" value="JOIN")
        input(type="button" value="CANCEL" onclick="location.href=`/`")
